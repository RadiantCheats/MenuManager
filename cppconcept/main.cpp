#include <iostream>
#include <Windows.h>
#include <string>
#pragma comment(lib, "urlmon.lib")

int main()
{
    std::cout << "a" << "\n";
    /*
    std::string url = "https://cherax.vip/loader.php?download=true";
    std::string path = "C:\\tmp\\CheraxLoader.zip";
    URLDownloadToFile(NULL, url.c_str(), path.c_str(), 0, NULL);
    */
    return 0;
}